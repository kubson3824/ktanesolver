import{ModuleType,type BombEntity}from"../../types";import R from"./RequestedModuleSolver";export default({bomb}:{bomb:BombEntity|null|undefined})=><R bomb={bomb}moduleType={ModuleType.COOKIE_JARS}name="Cookie Jars"example={{jars:[{cookie:0,count:3},{cookie:4,count:2},{cookie:9,count:1}],currentJar:1}}instructions="Cookie numbers use manual order 0–9. After eating the recommended cookie, reduce that jar's count and solve again. Navigation commands are conditional on the currently shown jar."/>;
