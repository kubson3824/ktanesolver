import{ModuleType,type BombEntity}from"../../types";import R from"./RequestedModuleSolver";const images=Array.from({length:15},(_,i)=>`/question-mark/${Math.floor(i/5)+1}-${i%5+1}.png`);export default({bomb}:{bomb:BombEntity|null|undefined})=><R bomb={bomb}moduleType={ModuleType.QUESTION_MARK}name="Question Mark"example={{symbols:[1,2,3,4]}}instructions="Use the numbered symbol reference. Symbol 5 is the death symbol and cannot appear. Release is a separate, conditional Twitch action after holding."symbolImages={images}/>;
