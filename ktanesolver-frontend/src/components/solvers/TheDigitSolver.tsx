import{useCallback,useMemo,useState}from"react";import{solveTheDigit,type TheDigitOutput}from"../../services/theDigitService";import{useRoundStore}from"../../store/useRoundStore";import{ModuleType,type BombEntity}from"../../types";import{generateTwitchCommand}from"../../utils/twitchCommands";import{ErrorAlert,SolverControls,SolverInstructions,SolverLayout,SolverSection,TwitchCommandDisplay,useSolver,useSolverModulePersistence}from"../common";export default function TheDigitSolver({bomb}:{bomb:BombEntity|null|undefined}){const[result,setResult]=useState<TheDigitOutput|null>(null),[twitchCommand,setTwitchCommand]=useState("");const{isLoading,error,isSolved,setIsLoading,setError,setIsSolved,clearError,reset:resetSolverState,currentModule,round,markModuleSolved}=useSolver();const updateModuleAfterSolve=useRoundStore(s=>s.updateModuleAfterSolve);const state=useMemo(()=>({result,twitchCommand}),[result,twitchCommand]);useSolverModulePersistence<typeof state,TheDigitOutput>({state,onRestoreState:useCallback(s=>{if(s.result)setResult(s.result);if(s.twitchCommand)setTwitchCommand(s.twitchCommand);},[]),onRestoreSolution:useCallback((solution:TheDigitOutput)=>{setResult(solution);setTwitchCommand(generateTwitchCommand({moduleType:ModuleType.THE_DIGIT,result:solution}));},[]),currentModule,setIsSolved});const solve=async()=>{if(!round?.id||!bomb?.id||!currentModule?.id)return setError("Missing required information");clearError();setIsLoading(true);try{const response=await solveTheDigit(round.id,bomb.id,currentModule.id);const command=generateTwitchCommand({moduleType:ModuleType.THE_DIGIT,result:response.output});setResult(response.output);setTwitchCommand(command);setIsSolved(response.solved);if(response.solved)markModuleSolved(bomb.id,currentModule.id);updateModuleAfterSolve(bomb.id,currentModule.id,{result:response.output,twitchCommand:command},response.output,response.solved);}catch(cause){setError(cause instanceof Error?cause.message:"Failed to solve The Digit");}finally{setIsLoading(false);}};return<SolverLayout><SolverControls onSolve={solve}onReset={()=>{setResult(null);setTwitchCommand("");resetSolverState();}}isLoading={isLoading}isSolved={isSolved}solveText="Calculate digit"/><ErrorAlert error={error}/>{result&&<SolverSection title="Submit digit"className="border-emerald-500/40"><p className="font-mono text-5xl font-bold">{result.answer}</p><p className="mt-2 text-xs text-muted-foreground">Intermediate values: {result.steps.join(" → ")}</p></SolverSection>}{twitchCommand&&<TwitchCommandDisplay command={twitchCommand}/>}<SolverInstructions>The Twitch command cycles upward from the current display and presses the screen. A wrong submission strikes and randomizes only the displayed cursor; the target digit remains unchanged.</SolverInstructions></SolverLayout>}
