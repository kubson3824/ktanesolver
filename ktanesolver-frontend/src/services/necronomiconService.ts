import{solveModule}from"../lib/api";export interface NecronomiconOutput{chapter:number;page:number;validChapters:number[];priority:number[];command:string}export const solveNecronomicon=(roundId:string,bombId:string,moduleId:string,origin:string,worshippedGod:string,referenceType:string,chapters:number[]):Promise<{output:NecronomiconOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{origin,worshippedGod,referenceType,chapters});
