import{solveModule}from"../lib/api";export type IkeaColor="BLUE"|"YELLOW";export interface IkeaButton{label:string;color:IkeaColor}export interface IkeaOutput{presses:number[];path:string;buttonDirections:string[];storeNumber:number}export const solveIkea=(roundId:string,bombId:string,moduleId:string,buttons:IkeaButton[]):Promise<{output:IkeaOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{buttons});
