import{solveModule}from"../lib/api";export interface FourCardMonteOutput{hand:string;coinPosition:number;coinValue:number;originalCardPosition:number;finalCardPosition:number;payment:string;commands:string[]}export interface FourCardMonteInput{cards:string[];coinColors:string[];finalOrder:number[];initialSeconds:number;dealSeconds:number;localHour:number;localDay:number;cardNumber:string;hasRoyalFlushModule:boolean;hasPoker:boolean;hasModulo:boolean;hasBritishSlang:boolean;hasFlipTheCoin:boolean;hasBlackjack:boolean}export const solveFourCardMonte=(roundId:string,bombId:string,moduleId:string,input:FourCardMonteInput):Promise<{output:FourCardMonteOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,input);
