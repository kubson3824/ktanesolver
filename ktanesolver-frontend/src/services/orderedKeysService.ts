import {solveModule} from "../lib/api";export const ORDERED_KEYS_COLORS=["RED","GREEN","BLUE","CYAN","MAGENTA","YELLOW"] as const;export type OrderedKeysColor=typeof ORDERED_KEYS_COLORS[number];export interface OrderedKey{keyColor:OrderedKeysColor;labelColor:OrderedKeysColor;label:number}export interface OrderedKeysOutput{stage:number;ranks:number[];pressOrder:number[];twitchCommand:string}export const solveOrderedKeys=(roundId:string,bombId:string,moduleId:string,stage:number,keys:OrderedKey[]):Promise<{output:OrderedKeysOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{stage,keys});
