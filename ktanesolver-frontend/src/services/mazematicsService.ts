import {solveModule} from "../lib/api";export interface MazematicsOutput{restrictedValues:string;moves:string[];runningValues:number[];twitchCommand:string}export const solveMazematics=(roundId:string,bombId:string,moduleId:string,shapeValues:number[],startCoordinate:string,initialValue:number,goalValue:number):Promise<{output:MazematicsOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{shapeValues,startCoordinate,initialValue,goalValue});
