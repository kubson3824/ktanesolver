import{solveModule}from"../lib/api";export const BASE_OPERATORS=["ADD","SUBTRACT","MULTIPLY","DIVIDE"]as const;export interface BasesOutput{answer:string;firstBase:number;secondBase:number;answerBase:number;firstDecimal:number;secondDecimal:number}export const solveBases=(roundId:string,bombId:string,moduleId:string,input:Record<string,unknown>):Promise<{output:BasesOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,input);
