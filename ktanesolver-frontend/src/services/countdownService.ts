import{solveModule}from"../lib/api";export interface CountdownOperation{left:number;operator:string;right:number;result:number}export interface CountdownOutput{operations:CountdownOperation[];expression:string;target:number}export const solveCountdown=(roundId:string,bombId:string,moduleId:string,target:number,numbers:number[]):Promise<{output:CountdownOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{target,numbers});
