import{solveModule}from"../lib/api";export interface EncryptedMorseOutput{response:string;responseMorse:string;intermediateMessage:string;decryptedMessage:string;sequenceB:string;swapped:boolean}export const solveEncryptedMorse=(roundId:string,bombId:string,moduleId:string,binarySequence:string,receivedMessage:string,receivedKey:string):Promise<{output:EncryptedMorseOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{binarySequence,receivedMessage,receivedKey});
