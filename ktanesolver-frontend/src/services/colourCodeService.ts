import{solveModule}from"../lib/api";export interface ColourCodeOutput{digits:number[];colors:string[];sequence:string[];entryCommand:string;submitCommand:string;specialSubmitTiming:boolean}export const solveColourCode=(roundId:string,bombId:string,moduleId:string,backgroundColor:string,currentMonth:number,currentHour:number):Promise<{output:ColourCodeOutput;solved:boolean}>=>solveModule(roundId,bombId,moduleId,{backgroundColor,currentMonth,currentHour});
