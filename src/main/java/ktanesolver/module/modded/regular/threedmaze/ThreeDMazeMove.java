package ktanesolver.module.modded.regular.threedmaze;

/**
 * Move instruction for the defuser in the 3D maze (same semantics as Mouse in The Maze).
 */
public enum ThreeDMazeMove {
	FORWARD,
	TURN_LEFT,
	TURN_RIGHT
}
