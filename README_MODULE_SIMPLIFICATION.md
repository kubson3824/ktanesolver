# Module solver framework

The module framework uses `@ModuleInfo` and `AbstractModuleSolver` to remove repeated registry and catalog boilerplate.

The maintained implementation guide is [docs/implementing-new-module-solver.md](docs/implementing-new-module-solver.md). This file remains only as a compatibility pointer for older links.
